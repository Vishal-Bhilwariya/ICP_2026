2696. Minimum String Length After Removing Substrings

https://leetcode.com/problems/minimum-string-length-after-removing-substrings/submissions/1889530266/

class Solution {
    public int minLength(String s) {
        Stack<Character> st = new Stack<>();
        for(char ch : s.toCharArray()){
            if(!st.isEmpty()){
                int c = st.peek();
                if((c=='A' && ch == 'B') || (c=='C' && ch == 'D')){
                    st.pop();
                    continue;
                }
            }
            st.push(ch);
        }
        return st.size();
    }
}