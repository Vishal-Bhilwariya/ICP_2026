 15. 3Sum 

LINK - https://leetcode.com/problems/3sum/submissions/1883669349/ 

------------------
CODE
-----------------
class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        List<List<Integer>> list = new ArrayList<>();
        Arrays.sort(nums);
        int n  = nums.length;
        for(int i=0;i<n-2;i++){
            if(i>0 && nums[i-1] == nums[i]) continue;
            for(int j = i+1 ; j<n-1 ; j++){
                if(j>i+1 && nums[j-1] == nums[j]) continue;
                int newtar = - (nums[i] + nums[j]);
                int idx = binarySearch(nums,j+1,n-1,newtar) ;
                if(idx != -1){
                    List<Integer> temp = new ArrayList<>();
                    temp.add(nums[i]);
                    temp.add(nums[j]);
                    temp.add(nums[idx]);
                    list.add(temp);
                }
            }
        }
        return list;
    }
    public int binarySearch(int[] nums,int left,int right,int tar){
        while(left<=right){
            int mid = (left+right)/2;
            if(nums[mid]==tar) return mid;
            else if(nums[mid]>tar) right = mid-1;
            else left = mid+1;
        }
        return -1;
    }
}