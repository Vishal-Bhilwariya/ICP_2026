2073. Time Needed to Buy Tickets

Link --> https://leetcode.com/problems/time-needed-to-buy-tickets/submissions/1899625663/

-------
Code
------

class Solution {
    public int timeRequiredToBuy(int[] tickets, int k) {
        int res = 0;
        int n = tickets.length;
        Queue<int[]> qu = new LinkedList<>();
        for(int i=0;i<n;i++) qu.add(new int[]{i,tickets[i]});

        while(!qu.isEmpty()){
            int[] temp = qu.remove();
            temp[1]--;
            if(temp[0]==k && temp[1]==0) return res+1;
            res++;
            if(temp[1]!=0) qu.add(temp);
        }
        return res;
    }
}